{
    "version": 2,
    "builds": [
        {
            "src":"build",
            "use":"@vercel/next"
        }
    ],
    "routes": [
        {
            "src":"/(.*)",
            "dest":"/build/$1"
        }
    ]
}